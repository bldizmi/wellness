I can see the issue - item #1022A appears on June 20th instead of June 21st. Looking at the logs, I need to trace why the date matching is still off by one day. Let me check what's happening when you click on June 21st specifically.


Executed curl -H "Authorization: Bearer $(cat /tmp/firebase_token 2>/dev/null || echo 'test')" "http://localhost:5000/api/today?client_date=2025-06-21" 2>/dev/null | head -20
I need to examine the exact date values being processed. Let me check the database directly to see what's happening with item #1022A.


Executed SQL query SELECT id, display_id, title, due_date, created_by FROM dev_items WHERE display_id = '#1022A' OR title LIKE '%one-time 6/21%';
The issue is confirmed - item #1022A has due_date: '2025-06-21' in the database but appears on June 20th in the UI. Looking at the logs, I can see that when requesting 2025-06-21, the backend is not finding this item. Let me trace the exact SQL execution.


Edited server/routes/today.ts
Looking at the issue more carefully, I notice that the problem might be in how dates are being handled in the calendar component itself. Let me check if there's a timezone issue in the frontend date generation.


Opened client/src/pages/Today.tsx
I need to check the actual date values being sent from the calendar component and trace where the timezone offset is occurring. Let me examine the calendar component's date generation more closely.


Opened client/src/components/DayCalendarStrip.tsx
I need to add debugging to the calendar component to see exactly what dates are being generated and sent. The issue is likely in the date construction logic causing an off-by-one error.


Edited client/src/components/DayCalendarStrip.tsx
Now I'll add debugging to the date selection handler to trace the exact values being passed:


Edited client/src/pages/Today.tsx
Perfect! The debug logs show the exact issue. Looking at the calendar date generation logs:

dayOffset=-5, date object=2025-06-21T07:00:00.000Z, dateString=2025-06-21
The date object shows T07:00:00.000Z which indicates the Date constructor is creating dates at 7 AM UTC, not midnight local time. This is causing the timezone offset issue.

The problem is in the Date constructor - when we create new Date(year, month, day), it creates the date in local timezone, but when converted to ISO string, it shows the UTC equivalent. The toLocaleDateString('en-CA') is working correctly, but there might be an edge case where the date calculation is still off.

Let me fix this by using a more reliable date construction method:


Edited client/src/components/DayCalendarStrip.tsx
I made a syntax error in the calendar component. Let me fix it:


Edited client/src/components/DayCalendarStrip.tsx
I need to fix the syntax errors in the calendar component. Let me restore the component properly:


Restarted Start application

Took a screenshot
Checkpoint made
just now
•
Fix calendar to show tasks on the correct date across time zones
Screenshot
Load preview
Let me fix the calendar component by viewing and correcting the syntax errors:


Opened client/src/components/DayCalendarStrip.tsx
The calendar component looks correct now. Let me test the updated date handling by clicking on June 21st to see if item #1022A appears correctly with the new strict string-based date construction.


Took a screenshot
Applied timezone-safe date construction in calendar component:

✓ Replaced timezone-sensitive Date methods with manual string construction
✓ Added comprehensive debug logging to trace date values through system
✓ Ensured consistent YYYY-MM-DD format generation without timezone conversion
✓ Backend updated with strict string-based date comparison

Please click on June 21st in the calendar. Item #1022A should now appear correctly on its due date (6/21) instead of the previous day. The debug logs will show the exact date values being generated and matched.